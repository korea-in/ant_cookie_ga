let ONE_GEN_ANGLE_COUNT = 350; // 한 세대당 유전자에 각도 개수
let ONE_GEN_ANT_COUNT = 50; // 한 세대당 개미 마리 수

let CANVAS_WIDTH = 500; // 캔버스의 가로 길이
let CANVAS_HEIGHT = 500; // 캔버스의 세로 길이
let COOKIE_PADDING = 50; // 쿠키가 생성되지 않는 폭의 너비

let ANT_ROTATE_ANGLE = 10; // 1 걸음당 회전 가능한 각도
let ANT_SETP_SIZE = 1.1; // 1 걸음당 거리
let ANT_START_DELAY = 6; // 개미 한 마리 출발 후 얼만큼 뒤에 다음 개미가 출발할 것인지 딜레이 계수
let MUTATION_COEFFICIENT = 0.005;